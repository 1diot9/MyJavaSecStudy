<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           https://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context
                           https://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/mvc
                           https://www.springframework.org/schema/mvc/spring-mvc.xsd">
    <!--
        设置该标签以后
        SpringMVC上下文中定义一个org.springframework.web.servlet.resource.DefaultServletHttpRequestHandler
        它会对进入DispatcherServlet的URL进行筛查
        静态资源由Web服务器默认的Servlet处理
        非静态资源的请求,由DispatcherServlet处理
     -->
    <!--
        设置该标签可以解决静态资源访问不到的问题
        例如访问: http://xxx.com/1.txt
        虽然存在但是访问不到1.txt,显示404的问题
    -->
    <mvc:default-servlet-handler/>

    <!-- 注解驱动,以使得访问路径与方法的匹配可以通过注解配置 -->
    <mvc:annotation-driven/>

    <!-- 扫描组件,将目录 ./SpringMVCTest/src/main/com/test/controller/ -->
    <!-- 加上@Controller注解的类作为SpringMVC的控制层 -->
    <context:component-scan base-package="test.controller"></context:component-scan>

    <!-- 配置视图解析器 -->
    <!-- 作用: 将prefix + 视图名称 + suffix = 确定最终要跳转的页面 -->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/view/"></property>
        <property name="suffix" value=".jsp"></property>
    </bean>

    <mvc:annotation-driven>
        <!-- 使用消息转换器,解决@ResponseBody注解返回中文乱码问题 -->
        <mvc:message-converters register-defaults="true">
            <bean class="org.springframework.http.converter.StringHttpMessageConverter">
                <property name="supportedMediaTypes" value="text/html;charset=UTF-8"/>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>

    <!-- 配置文件上传解析器 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <!-- 设置上传文件的最大尺寸为5MB -->
        <property name="maxUploadSize">
            <value>5242880</value>
        </property>
    </bean>

    <!-- 配置拦截器 -->
    <mvc:interceptors>
        <!-- 配置一个全局拦截器,拦截所有请求 -->
        <!-- 所有SpringMVC请求都会先进入这些拦截器 -->
        <bean class="test.interceptor.TestAllHandlerInterceptor"/>

        <!-- 配置一个拦截"/gotoTest"的路径的拦截器 -->
        <mvc:interceptor>
            <!-- 配置拦截器作用的路径 -->
            <mvc:mapping path="/gotoTest"/>
            <!-- 定义<mvc:interceptor>元素中,表示该拦截器的执行方法 -->
            <bean class="test.interceptor.Test2HandlerInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>
</beans>